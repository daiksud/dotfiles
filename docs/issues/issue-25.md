# Issue 25: .gitconfig のインデントをハードタブに統一

## 日付

2026-04-20

## 背景・目的

`.gitconfig` のインデントがスペースとタブで混在していたため、ハードタブに統一する。

## 変更内容

- `dotfiles/.gitconfig`: 行頭の半角スペース 2 つをすべてハードタブ (`\t`) に置換

## 備考

`git config` が生成するファイルはデフォルトでタブを使用するため、それに合わせた統一。
