# Issue 29: Brewfile の依存パッケージを整理

## 日付

2026-04-21

## 背景・目的

CLI 環境で利用するツールセットの構成を見直し、必要なパッケージの追加と不要なパッケージの削除を反映するため。

## 変更内容

- `Brewfile`
  - `brew "jq"` を通常の CLI ツール群へ移動
  - `cask "font-noto-sans-symbols-2" if OS.mac?` を削除

## 備考

- 依存一覧の並び順も変更差分に含まれるが、実質的な変更は上記2点。
